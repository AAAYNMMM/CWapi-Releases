# Debugging

Description: 用于错误定位、日志分析和故障修复。

## Rules

- 先根据错误、日志和复现条件确认故障发生在哪一层，再改代码。
- 区分症状、直接错误和根因；优先修根因，不用绕过验证来掩盖问题。
- 尽量先建立最小复现或最接近原故障的验证路径，一次改变一个主要变量。
- 工具或命令失败后先分析返回结果，区分参数、环境、项目逻辑和协议问题，不盲目重复同一个失败操作。
- 修复后必须验证原故障路径，并检查最接近的回归风险。
