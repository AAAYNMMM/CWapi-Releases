# Git

Description: 用于 Git 仓库、分支、提交、合并和远程操作。

## Rules

- 操作前确认当前分支、HEAD、工作区状态和目标 ref。
- 不覆盖用户未提交的工作，不擅自 reset、clean、force push 或删除远程 ref。
- 提交应只包含当前任务相关改动，提交信息准确描述变化。
- 对 commit、merge、push、tag、删除等有副作用操作，如果工具结果不确定，不要直接重复；先检查 Git/远端当前状态确认是否已生效。
- 合并或推送前再次检查 diff、适用的验证证据和目标分支，并严格遵守当前任务授权范围。
